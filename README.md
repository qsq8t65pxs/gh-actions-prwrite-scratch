# gh-scratch

Minimal GitHub Actions scratch repo for validating:

- whether `github-actions[bot]` can submit an `APPROVE` review with only `pull-requests: write`
- whether the same token can enable native auto-merge


trigger
